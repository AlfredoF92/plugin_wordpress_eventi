# cral_2.0 (WordPress)

This repository contains a WordPress project snapshot for development purposes.

## Notes

- `wp-config.php` and runtime/generated content (uploads, caches, logs) are excluded via `.gitignore`.
- The paid plugin `elementor-pro` is excluded.

